<h1><%= @story_list.name %></h1>

<% if @story_list.adventures.any? %>
  <h4>Here are the Adventures for this list:</h4>

  <ul>
    <% @story_list.adventures.each do |adventure| %>
      <li>

        <%= adventure.name %> <%= adventure.description %>  <%= adventure.image %> |
        <%= link_to "Edit", edit_list_task_path(@list, task) %> |
        <%= link_to "Delete", list_task_path(@list, task),
                                 :data => {:confirm => "Are you sure?",
                                                :method => "delete"} %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p> There are no adventures yet! </p> 
<% end %>










<p><%= link_to "Edit", edit_story_list_path(@story_list) %></p>
<p><%= link_to "Delete", story_list_path(@story_list),
                         :data => {:confirm => "You sure?",
                                   :method => "delete"} %></p>

<p><%= link_to "Return to list of Stories",  story_lists_path  %></p>
